services:
  - type: web
    name: nest-api
    env: node
    buildCommand: NODE_OPTIONS="--max-old-space-size=2048" npm run build
    startCommand: npm run start:prod
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
